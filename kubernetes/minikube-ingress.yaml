apiVersion: extensions/v1beta1
kind: Ingress
metadata:
  name: test-ingress
  annotations:
      nginx.ingress.kubernete.io/rewrite-target: /
spec:
  backend:
    serviceName: spark-master
    servicePort: 8080
